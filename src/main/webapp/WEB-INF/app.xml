<?xml  version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       ">

    <context:property-placeholder location="classpath:config.properties"/>

    <bean id="messageService" class="ru.javazen.telegram.bot.MessageService">
        <property name="token" value="${bot.token}"/>
        <property name="hookUrl" value="${bot.hook_url}"/>
    </bean>

    <bean class="ru.javazen.telegram.bot.HookService">
        <property name="messageService" ref="messageService"/>
    </bean>

    <bean class="ru.javazen.telegram.bot.BotInitializer" init-method="init">
        <property name="token" value="${bot.token}"/>
        <property name="hookUrl" value="${bot.hook_url}"/>
    </bean>

</beans>